{% extends 'base.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-10 offset-md-2 m-b-2">
            <ul class="list-group">
                {% for post in posts_with_comments %}
                    <li class="list-group-item">
                        <div>
                            <h4>Имя: {{ post.author }}</h4>
                            <p>{{ post.text }}</p>
                        </div>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>

    <div class="row">
        {{ show_massage() }}
        {% for post in posts %}
            <div class="jumbotron">
                <div class="row">
                    <div class="col-md-2">
                       Имя автора:
                    </div>
                    <div class="col-md-10">
                        <h1><a href="/post/show/{{ post.id }}">{{ post.author }}</a></h1>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-2">
                        Течт:
                    </div>
                    <div class="col-md-10">
                        <p class="lead">{{ post.text }}</p>
                        <div>Дата: {{ post.created|date('d.m.Y') }}</div>
                        <div>Количество комментариев: {{ post.comment_count }}</div>
                        <div><a href="/post/show/{{ post.id }}">Просмотреть</a> </div>
                    </div>
                </div>
            </div>
        {% endfor %}
        <div id="preview-bloc"></div>
    </div>
    <div class = "row">
        <form id="form" action="/post/save" method="post" enctype="multipart/form-data">

            <div class="form-group">
                <input type="text" name="name" class="form-control" id="name" placeholder="Имя">
                <small id="emailHelp" class="form-text text-muted">На фронтэнде валидация: ^[a-zA-Z0-9]+$ на бекенде ^[a-zA-Z]+$ - для демонстрации двойной валидации</small>
            </div>
            <div class="form-group">
                <textarea name="text" class="form-control" id="text" rows="3" placeholder="Текст поста"></textarea>
            </div>

            <button type="submit" name="submit" id="send-submit" class="btn btn-primary">Сохранить</button>
        </form>
    </div>

{% endblock %}

{% block js %}
    <script>
        // just for the demos, avoids form submit
        $.validator.addMethod(
                "regex",
                function(value, element, regexp) {
                    var re = new RegExp(regexp);
                    return this.optional(element) || re.test(value);
                },
                "Введены неразрешонные символы."
            );
        $.validator.addMethod(
                'filesize',
                function(value, element, param) {
                    return this.optional(element) || (element.files[0].size <= param)
                });

        function validate_form() {
            return $( "#form" ).validate({
                rules: {
                    name: {
                        required: true,
                        regex: '^[а-яА-Яa-zA-Z0-9 ]+$'
                    },
                    text: {
                        required: true
                    },
                },
                messages: {
                    fileToUpload: "Файл должен иметь формат JPG, GIF или PNG, размер должен быть меньше 1MB"
                }
            });
        }
        validate_form();
    </script>
{% endblock %}
